"""Calibration experiment tools (L2-005 / L3-004) — CPU-safe contracts.

Data, features, and reports stay outside the git tree. This package only
implements split isolation, threshold selection rules, and runners that accept
injected scorers.
"""
