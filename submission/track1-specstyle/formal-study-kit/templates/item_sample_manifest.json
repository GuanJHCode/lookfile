{
  "schema_version": "specstyle.calibration.sample_manifest.v2",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "study_plan_sha256": "${STUDY_PLAN_SHA256}",
  "observation_unit": "item",
  "samples": [
    {"sample_id": "${SAMPLE_ID}", "candidate_sha256": "${CANDIDATE_SHA256}", "source_family_sha256": "${SOURCE_FAMILY_SHA256}", "reference_family_sha256": "${REFERENCE_FAMILY_SHA256}", "isolation_group_sha256": "${ISOLATION_GROUP_SHA256}", "split": "${calibration_OR_validation_OR_test}", "annotation_record_sha256": "${ANNOTATION_RECORD_SHA256}", "provenance_record_sha256": "${PROVENANCE_RECORD_SHA256}"}
  ]
}
