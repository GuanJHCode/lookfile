{
  "schema_version": "specstyle.calibration.metric_production_approval.v1",
  "approval_id": "${APPROVAL_ID}",
  "approved": true,
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "study_id": "${STUDY_ID}",
  "layer": "${L2_OR_L3}",
  "observation_unit": "${item_OR_batch}",
  "metric_id": "${METRIC_ID}",
  "operator": "${gte_OR_lte}",
  "threshold": 0.0,
  "implementation_pin": {"id": "${IMPLEMENTATION_ID}", "revision": "${IMPLEMENTATION_REVISION}", "sha256": "${IMPLEMENTATION_SHA256}"},
  "binding_pin": {"id": "${BINDING_ID}", "revision": "${BINDING_REVISION}", "sha256": "${BINDING_SHA256}"},
  "verifier_pin": {"id": "${VERIFIER_ID}", "revision": "${VERIFIER_REVISION}", "sha256": "${VERIFIER_SHA256}"},
  "preprocessor_pin": {"id": "${PREPROCESSOR_ID}", "revision": "${PREPROCESSOR_REVISION}", "sha256": "${PREPROCESSOR_SHA256}"},
  "prepared_evidence_sha256": "${PREPARED_EVIDENCE_SHA256}",
  "test_reveal_sha256": "${TEST_REVEAL_SHA256}",
  "annotation_protocol_sha256": "${ANNOTATION_PROTOCOL_SHA256}",
  "approver_id": "guan",
  "issued_at": "${UTC_TIMESTAMP}"
}
