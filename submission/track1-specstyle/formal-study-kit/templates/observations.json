{
  "schema_version": "specstyle.calibration.observations.v2",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "study_plan_sha256": "${STUDY_PLAN_SHA256}",
  "sample_manifest_sha256": "${SAMPLE_MANIFEST_SHA256}",
  "split": "${calibration_OR_validation_OR_test}",
  "observation_unit": "${item_OR_batch}",
  "metric_id": "${METRIC_ID}",
  "operator": "${gte_OR_lte}",
  "observations": [
    {"sample_id": "${SAMPLE_ID}", "sample_binding_sha256": "${CANDIDATE_OR_COHORT_SHA256}", "score": 0.0, "label_positive": false, "annotation_record_sha256": "${ANNOTATION_RECORD_SHA256}"}
  ]
}
