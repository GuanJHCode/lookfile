{
  "schema_version": "specstyle.calibration.profile_approval.v1",
  "approval_id": "${L3_PROFILE_APPROVAL_ID}",
  "approved": true,
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "source": "l3",
  "threshold_profile_pin": {"id": "${L3_PROFILE_ID}", "revision": "${L3_PROFILE_REVISION}", "sha256": "${L3_PROFILE_SHA256}"},
  "metric_approval_sha256s": ["${STRUCTURE_METRIC_APPROVAL_SHA256}"],
  "approver_id": "guan",
  "issued_at": "${UTC_TIMESTAMP}"
}
