{
  "schema_version": "specstyle.calibration.profile_approval.v1",
  "approval_id": "${L2_PROFILE_APPROVAL_ID}",
  "approved": true,
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "source": "l2",
  "threshold_profile_pin": {"id": "${L2_PROFILE_ID}", "revision": "${L2_PROFILE_REVISION}", "sha256": "${L2_PROFILE_SHA256}"},
  "metric_approval_sha256s": ["${BATCH_METRIC_APPROVAL_SHA256}", "${ITEM_METRIC_APPROVAL_SHA256}"],
  "approver_id": "guan",
  "issued_at": "${UTC_TIMESTAMP}"
}
