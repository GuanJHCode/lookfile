{
  "schema_version": "specstyle.calibration.test_commitment.v2",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "study_plan_sha256": "${STUDY_PLAN_SHA256}",
  "sample_manifest_sha256": "${SAMPLE_MANIFEST_SHA256}",
  "observation_unit": "${item_OR_batch}",
  "sealed_test_observations_sha256": "${SEALED_TEST_OBSERVATIONS_SHA256}",
  "sample_ids": ["${ORDERED_TEST_SAMPLE_ID}"],
  "sample_bindings_sha256": "${ORDERED_TEST_BINDINGS_SHA256}",
  "positive_count": 40,
  "negative_count": 40
}
