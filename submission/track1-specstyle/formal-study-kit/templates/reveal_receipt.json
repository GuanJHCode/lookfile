{
  "schema_version": "specstyle.calibration.reveal_receipt.v2",
  "receipt_id": "${RECEIPT_ID}",
  "study_id": "${STUDY_ID}",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "approval_kind": "REVEAL_TEST",
  "approved": true,
  "validation_report_sha256": "${PREPARED_EVIDENCE_SHA256}",
  "sealed_test_observations_sha256": "${SEALED_TEST_OBSERVATIONS_SHA256}",
  "approver_id": "${INDEPENDENT_REVEAL_APPROVER_ID}",
  "issued_at": "${UTC_TIMESTAMP}"
}
