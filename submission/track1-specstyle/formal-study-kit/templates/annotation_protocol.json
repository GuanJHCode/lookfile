{
  "schema_version": "specstyle.annotation_protocol.v2",
  "protocol_id": "${PROTOCOL_ID}",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "observation_unit": "${item_OR_batch}",
  "metric_id": "${METRIC_ID}",
  "label_definition": "${FROZEN_BINARY_LABEL_DEFINITION}"
}
