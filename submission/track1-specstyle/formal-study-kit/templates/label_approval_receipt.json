{
  "schema_version": "specstyle.calibration.approval_receipt.v2",
  "receipt_id": "${RECEIPT_ID}",
  "study_id": "${STUDY_ID}",
  "target_cell_sha256": "${TARGET_CELL_SHA256}",
  "approval_kind": "HUMAN_LABELS",
  "approved": true,
  "label_source": "HUMAN_APPROVED",
  "observation_unit": "${item_OR_batch}",
  "study_plan_sha256": "${STUDY_PLAN_SHA256}",
  "sample_manifest_sha256": "${SAMPLE_MANIFEST_SHA256}",
  "annotation_protocol_sha256": "${ANNOTATION_PROTOCOL_SHA256}",
  "observation_sha256s": ["${CALIBRATION_OBSERVATIONS_SHA256}", "${VALIDATION_OBSERVATIONS_SHA256}", "${SEALED_TEST_OBSERVATIONS_SHA256}"],
  "approver_id": "${INDEPENDENT_LABEL_APPROVER_ID}",
  "issued_at": "${UTC_TIMESTAMP}"
}
